include:
  - ./postgres/docker-compose.yml
  - ./rustfs/docker-compose.yml
  - ./mlflow/docker-compose.yml
  - ./mlflow-admin/docker-compose.yml
  - ./nginx/docker-compose.yml

networks:
  mlops:
    name: ${MLOPS_NETWORK}
    driver: bridge
